<UserControl
    x:Class="AtEase.App.Views.DesktopBrowserView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="Simplified Browsing"
            FontSize="28"
            FontWeight="SemiBold"
            Margin="0,0,0,12" />

        <TextBlock Grid.Row="1" Text="{Binding StatusMessage}" Margin="0,0,0,12" />

        <StackPanel Grid.Row="2" Orientation="Vertical" Spacing="8">
            <TextBlock Text="This mode is a Phase 1 placeholder." Opacity="0.7" />
            <TextBlock Text="Use Settings to enable and set the root folder." Opacity="0.7" />
        </StackPanel>
    </Grid>
</UserControl>
