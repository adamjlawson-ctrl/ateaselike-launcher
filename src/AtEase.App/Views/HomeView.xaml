<UserControl
    x:Class="AtEase.App.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="Launcher"
            FontSize="28"
            FontWeight="SemiBold"
            Margin="0,0,0,16" />

        <GridView
            Grid.Row="1"
            IsItemClickEnabled="False"
            ItemsSource="{Binding Tiles}"
            SelectionMode="None">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Button
                        Width="220"
                        Height="140"
                        Margin="8"
                        Command="{Binding LaunchCommand}">
                        <StackPanel Spacing="8">
                            <TextBlock Text="{Binding DisplayName}" FontSize="20" FontWeight="SemiBold" TextWrapping="WrapWholeWords" />
                            <TextBlock Text="{Binding Path}" Opacity="0.7" TextWrapping="Wrap" MaxLines="2" />
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</UserControl>
