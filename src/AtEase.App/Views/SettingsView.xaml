<UserControl
    x:Class="AtEase.App.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="Visible Tiles"
            FontSize="28"
            FontWeight="SemiBold"
            Margin="0,0,0,16" />

        <ListView Grid.Row="1" ItemsSource="{Binding VisibilityItems}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock
                            Grid.Column="0"
                            Text="{Binding DisplayName}"
                            VerticalAlignment="Center"
                            FontSize="18" />

                        <ToggleSwitch
                            Grid.Column="1"
                            Header="Visible"
                            IsOn="{Binding IsVisible, Mode=TwoWay}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12" Margin="0,16,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" MinWidth="120" Height="44" />
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Opacity="0.8" />
        </StackPanel>
    </Grid>
</UserControl>
