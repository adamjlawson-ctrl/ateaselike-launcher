<Window
    x:Class="AtEase.App.BrowseProofPickerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:AtEase.App.Models"
    mc:Ignorable="d"
    Title="Select App">

    <Grid Padding="16" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Select App" FontSize="20" FontWeight="SemiBold" />

        <Grid Grid.Row="1">
            <ListView x:Name="TestListView" SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:AppPickerEntry">
                        <StackPanel Spacing="2" Margin="0,3,0,3">
                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding LaunchPath}" FontSize="12" Opacity="0.72" TextWrapping="WrapWholeWords" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <TextBlock
                x:Name="NoAppsText"
                Text="No apps found"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="14"
                Visibility="Collapsed" />
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
            <Button Content="Cancel" Click="Cancel_Click" />
            <Button x:Name="SelectButton" Content="Select" Click="Select_Click" />
        </StackPanel>
    </Grid>
</Window>
